FROM node:20-alpine

WORKDIR /app
COPY . .

RUN echo "UI scaffold placeholder. Next step will initialize Next.js."

CMD ["sh", "-c", "echo 'UI not initialized yet. Next step: create Next.js app.' && sleep 3600"]